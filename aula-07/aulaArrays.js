// String, Number, Boolean, Object{}, Array[]

let times = ['Framengo', 'Viúvas de pele', 'Vascão', 'Coringão'];

// console.log(times[0])
times.push('Guarani da capital') //Adiciona um novo elemento no array
times.pop(2) // Remove último elemento do Array
// console.log(times)

// let time = times.filter(function(time) {
//     return time.includes('e');
// })

for (var contador = 0; contador <= 3; contador++) {
    console.log(times[contador])
}